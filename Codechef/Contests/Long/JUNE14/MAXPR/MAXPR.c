#include <stdio.h>

int brr[205][200005] = {0},crr[205][200005] = {0};

int main() {
    int t,n,arr[200005];
    scanf("%d",&t);
    while(t--) {
        scan("%d",&n);
        for(i=0;i<n;i++) {
            scanf("%d",&arr[i]);
        }
        for(i=0;i<n;i++) {
            for(j)
        }
    }
}
