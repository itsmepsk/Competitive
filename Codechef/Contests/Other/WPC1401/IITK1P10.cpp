#include <bits/stdc++.h>
using namespace std;

struct node {
    long long int data;
    node* left;
    node* right;
    node* parent;
}* root = NULL;

node* arr[100005];

int main() {
    cin >> n >> q;
    for(i=0;i<n;i++) {
        cin >> u >> v;
    }
}
