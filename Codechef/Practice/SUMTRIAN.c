#include <stdio.h>
int arr[100][100];

int max(int p)
{
	sum = max(p-1) + max()
}

int main()
{
	int t,c;
	scanf("%d",&t);
	for(c=0;c<t;c++)
	{
		scanf("%d",&n);
		for(i=0;i<n;i++)
		{
			for(j=0;j<i;j++)
			{
				scanf("%d",&arr[i][j]);
			}
		}
		m = max(n);
	}
}
