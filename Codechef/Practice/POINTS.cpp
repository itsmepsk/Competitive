#include <stdio.h>
#include <iostream>
#include <algorithm>

int main() {
	int t;
	scanf("%d",&t);
	while(t--) {
		scanf("%d",&n);
		for(i=0;i<n;i++) {
			scanf("%d %d",&arr[i],&brr[i]);
			crr[i] = arr[i];
		}
		sort(crr+0,crr+n);
		for(i=0;i<n;i++) {
			for()
		}
	}
}
